<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Knockout Example" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="#">Knockout.js Examples</a>
          <div class="container-fluid">
            <ul class="nav nav-pills">
              <li><%= link_to "Home", root_url  %></li>
              <li class="dropdown">
                <a href="#"
                      class="dropdown-toggle"
                      data-toggle="dropdown">
                      Examples
                      <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "#1 - Presenter", "/example/1"  %></li>
                  <li><%= link_to "#2 - Add User", "/example/2"  %></li>
                  <li><%= link_to "#3 - Shopping Cart", "/example/3"  %></li>
                </ul>
              </li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="content">
        <div class="row">
          <div class="span8">
            <%= yield %>
          </div>
          <% if content_for?(:view_model) %>
            <div class="span4">
              <div class="well">
                <h3>View Model</h3>
                <hr />
                <ul class="nav nav-list">
                  <%= content_for?(:view_model) ? yield(:view_model) : "" %>
                </ul>
              </div>
            </div><!--/span-->
          <% end %>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= content_for?(:init_js) ? yield(:init_js) : "" %>
  </body>
</html>
